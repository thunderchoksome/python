#C++ learning
* Day one
  
```

      #include <iostream>
      using namespace std;//using namespace std;   使用这个可以减免下面程序中输入输出时的前缀“std::”---告诉编译器使用 std 命名空间

      extern int c;//变量声明---您可以使用 extern 关键字在任何地方声明一个变量。虽然您可以在 C++ 程序中多次声明一个变量，但变量只能在某个文件、函数或代码块中被定义一次。

      int main()//在c语言中括号中不能填void，int main(void) 指的是此函数的参数为空，不能传入参数，如果你传入参数，就会出错。c++允许
      {
      cout << "Hello World!\n"; //输出"Hello World!\n"
	  int _1a$;//可见c++允许带有$做标识符；且$可以放到标识符开头
      typedef int llq;//格式：typedef type newname; typedef为一个已有的类型取一个新的名字
	  llq lfmq;
	  enum color {red, green ,blue};//枚举类型(enumeration)是C++中的一种派生数据类型，它是由用户定义的若干枚举常量的集合。原格式如下，若缺少[=整型常数]则第一个为0依次+1，也可由用户自定。且枚举类型不一定要在 main 中定义
	  /* enum 枚举名{ 
      标识符[=整型常数], 
      标识符[=整型常数], 
      ... 
      标识符[=整型常数]
      } 枚举变量;
     */
	 //C++ 中的左值（Lvalues）和右值（Rvalues） 左值是变量表达式，指向内存单元，右值是确定量
	 //在函数内，局部变量的值会覆盖全局变量的值
	 /*  数值
	    212       合法的
		215u        合法的
		0xFeeL      合法的，后缀可小写可大写
		078         非法的：8 不是八进制的数字
		032UU       非法的：不能重复后缀
	 */
	//培养习惯：常量定义为大写字母形式
	//for( ; ; ) 无限循环 可以按 Ctrl + C 键终止一个无限循环。
	Exp1 ? Exp2 : Exp3; //？：运算符? 表达式的值是由 Exp1 决定的。如果 Exp1 为真，则计算 Exp2 的值，结果即为整个 ? 表达式的值。如果 Exp1 为假，则计算 Exp3 的值，结果即为整个 ? 表达式的值。
	//引用内置函数----数学头文件 <cmath>。
      return 0;
      }
```
今天是第一天开始学习c++做的笔记，明天继续！明天相应的还要完成一道经典例题的编程。加油！
